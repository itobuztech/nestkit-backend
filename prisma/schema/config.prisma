enum AppConfigField {
  APP_NAME
  LOGO_URL
  FAVICON_URL
}

model AppConfig {
  id String @id @default(uuid())
  
  field AppConfigField @unique
  value String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  deletedAt DateTime?
}